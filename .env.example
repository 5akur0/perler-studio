ALLOWED_ORIGINS=http://localhost:5173
ADMIN_TOKEN=replace-me
# Secret key for HMAC-hashing stored client IPs in gallery submissions. Without
# it the server falls back to a plain SHA-256 (reversible over the small IPv4
# space), so set a long random value in production.
IP_HASH_SECRET=replace-me
