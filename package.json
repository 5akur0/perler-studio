{
  "name": "perler-studio",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "node server.js",
    "start": "node server.js",
    "build": "esbuild src/main.js --bundle --format=iife --outfile=app.bundle.js",
    "watch": "esbuild src/main.js --bundle --format=iife --outfile=app.bundle.js --watch"
  },
  "devDependencies": {
    "esbuild": "^0.28.0"
  }
}
